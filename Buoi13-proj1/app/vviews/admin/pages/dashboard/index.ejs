<%  
    let arrBox = [
        {name: 'Items', icon: 'ion-clipboard', link: `${systemConfig.prefixAdmin}/items`, total: totalItems},
        {name: 'Groups', icon: 'ion-ios-people', link: `${systemConfig.prefixAdmin}/groups`, total: totalGroups},
        {name: 'Users', icon: 'ion-ios-person', link: `${systemConfig.prefixAdmin}/users`, total: totalUsers},
        {name: 'Categories', icon: 'ion-clipboard', link: `${systemConfig.prefixAdmin}/categories`, total: totalCategories},
        {name: 'Articles', icon: 'ion-ios-book', link: `${systemConfig.prefixAdmin}/articles`, total: totalArticles}
    ];

    let xhtmlBox = '';
    arrBox.forEach((box) => {
        xhtmlBox += `<div class="col-lg-3 col-6">
                        <div class="small-box bg-info">
                            <div class="inner">
                                <h3>${box.total}</h3>
                                <p>${box.name}</p>
                            </div>
                            <div class="icon text-white">
                                <i class="ion ${box.icon}"></i>
                            </div>
                            <a href="${box.link}" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
                        </div>
                    </div>`;
    });   
%>

<section class="content">
    <div class="container-fluid">
        <div class="row justify-content-left">
            <%- xhtmlBox %>
        </div>
    </div>
</section>